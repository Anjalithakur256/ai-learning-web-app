rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Development rules: Allow all read/write for authenticated users
    // For production, replace with more restrictive per-collection rules
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
